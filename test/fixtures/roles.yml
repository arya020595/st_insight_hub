# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# ============================================================================
# ROLES - Based on Business Rules:
# - Superadmin: Full system access (bypasses all permission checks)
# - Client: Can only access dashboard management, view assigned projects
# ============================================================================

superadmin_role:
  name: Superadmin
  description: Full system access - bypasses all permission checks
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

client_role:
  name: Client
  description: Client users - can only access assigned projects and their dashboards
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>
